<%= form_for @bid, url: item_bids_path(@item), remote: true do |f| %>

  <strong><%= f.label :Price %></strong>
 <%= f.number_field :price %><br>
<br>
 <%# <button  class='btn btn-primary' value = 'rating' > Submit </button> %>
<div class="actions">
<%= f.submit "submit" %>
</div>
<%end%>

