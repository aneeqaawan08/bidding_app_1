<h1>Welcome to bidding app 1</h1>


<table class="table table-dark table-striped">
  <thead>
    <tr>
     
      <th>Title</th>
     <th>Category</th>
      <th>Price</th>
      <th>Description</th>
      <th>Image</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
  <%Item.all.each do |item|%>
        <tr>
        <td><%= item.title %></td>
        <td><%= item.category.title %></td>
        <td><%= item.price %></td>
        <td><%= item.description %></td>
        <%# <td><%= cl_image_tag(category.image.key, width: 200, height: 100, crop: :scale, effect: "cartoonify") if category.image.attached?%> </td> 
        <td> <%= image_tag (item.image.url),style:"width:50px" if item.image.present?%></td>
        <td><a herf="#" class="card-link" ><%= link_to 'Show', item %></a></td>
      <%end%>
  </tbody>
</table>

 <%if user_signed_in?%>
      <%# <li><a href="/users/sign_out" data-method="delete"> Log Out</a></li> %>
      <%# <td><%= link_to 'create new', new_category_path %> </td> 
      
      <%else%>
    <%# <td><%= link_to 'sign out', destroy_user_session_path, :method => 'delete' %> </td>  
    <%# <td><%= button_to "Log out", destroy_user_session_path, method: :delete %> </td>
   <%# <td><%= link_to 'sign_up', new_user_registration_path %> </td>  

   <%# <td><%= link_to 'sign_in', user_session_path %> </td>  
    <%end%>














