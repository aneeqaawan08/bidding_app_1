
<h3 class="text-center">Item Details</h3>
<div class= "container square-box d-flex justify-content-center">
<li>
<p> Title: <%= @item.title %> </p>
<p> Price: <%= @item.price%> </p>
<p> Category: <%= @item.category.title%> </p>
<p> Description: <%= @item.description %> </p>
<%# <p>image:<%= cl_image_tag(@category.image.key,width: 200, height: 100, crop: :scale, effect: "cartoonify") if @category.image.attached? %></p>
<%= image_tag (@item.image.url),style:"width:200px" if @item.image.present? %>

 <%# <div display: flex justify-content> %>

<% if user_signed_in? %>
<h4> Bidding price</h4>

<div id= "bid_pane">
<% @item.bids.each do |bid|%>
<%= render 'bids/bid', bid: bid%>
<br>

<%end%>
</div>

<div>
<h3> Add bidding</h3>
<%= render 'bids/form' %>
</div>

<%end%>


<% if user_signed_in?%>
<%= link_to 'Back', items_path %> 
<% else %>
<%= link_to 'Back', root_path %> 
<%end%>
</li>
</div>
